Fix World pos / local pos
Do Shape/Constraints updating

Add all constraints with examples in the scene
Add Shape "instancing"

Add Newt generation system (using marching cube and voxel ?)
Add Chain generation system (to make ropes, chains, bridges, ..)

Make a new Scene with a big demo of everything
When @Doprez PR is merged, make use of new Extensions.cs
Change Colliders to work the same way as ConstraintsV2 (using Bepu struc) (it will allow runtime edition out of the box and no GC)