// A utility shader referenced only through static calls, like the engine's LuminanceUtils.
namespace Stride.Shaders.Tests;

shader StaticCallUtils
{
    static float Weight(float3 color)
    {
        return dot(color, float3(0.299, 0.587, 0.114));
    }
};
