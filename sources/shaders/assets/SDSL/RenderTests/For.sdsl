// PSMain(ExpectedResult=#05050505)

namespace Stride.Shaders.Tests;

shader For
{
    stream float4 ColorTarget : SV_Target0;

    void PSMain()
    {
        streams.ColorTarget = float4(0.0, 0.0, 0.0, 0.0);
        for (int i = 0; i < 5; ++i)
        {
            streams.ColorTarget += float4(1.0, 1.0, 1.0, 1.0) / 255.0;
        }
    }
}