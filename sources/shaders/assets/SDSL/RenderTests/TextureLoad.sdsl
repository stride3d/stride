// PSMain(ExpectedResult=#22446688, texture.Texture1=#11223344)

namespace Stride.Shaders.Tests;

shader TextureLoad
{
    stream float4 ColorTarget : SV_Target0;
    stream float2 TexCoord : TEXCOORD;

    stage Texture2D<float4> Texture1;

    void PSMain()
    {
        streams.ColorTarget = Texture1.Load(int3(0, 0, 0)) + Texture1[int2(0,0)];
    }
}