<Project>
	<!-- We should only store settings that apply to All projects! -->
    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
		<GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    </PropertyGroup>
</Project>