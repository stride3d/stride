<Window x:Class="Xenko.ConfigEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xenko Configuration Editor" MinWidth="640" MinHeight="480" WindowStartupLocation="CenterScreen" Background="WhiteSmoke">

  <!--<Window.InputBindings>
    <KeyBinding Modifiers="Control" Key="S" Command="{Binding SaveCommand}"/>
    <KeyBinding Key="F5" Command="{Binding ReloadCommand}"/>
  </Window.InputBindings>-->

  <Grid UseLayoutRounding="True">
    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource RootView}" Focusable="False"/>
  </Grid>

</Window>
