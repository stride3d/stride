<sd:ModalWindow x:Class="Stride.Assets.Presentation.Templates.DotNetNewTemplateParametersWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:sd="http://schemas.stride3d.net/xaml/presentation"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                mc:Ignorable="d"
                Title="{Binding Title}" SizeToContent="Height" Width="520"
                Style="{DynamicResource WindowChromeStyle}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Stride.Core.Assets.Editor.Wpf;component/View/CommonResources.xaml"/>
                <!-- Platform DrawingImage resources keyed by ConfigPlatforms.<Platform>; used by the
                     specialized 'platforms' row to show OS icons next to each checkbox. -->
                <ResourceDictionary Source="../View/ImageDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel>
        <Border Background="{StaticResource EmphasisColorBrush}" Margin="20,20,20,0">
            <TextBlock Text="{Binding TemplateName}" Margin="20,10" FontSize="16"/>
        </Border>
        <TextBlock x:Name="DescriptionTextBlock" Text="{Binding TemplateDescription}" Margin="20,5,20,10" TextWrapping="Wrap"/>
        <StackPanel x:Name="ParametersPanel" Margin="20,0,20,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10,20,20">
            <Button Content="{sd:Localize OK}" IsDefault="True" Width="80" Margin="0,0,8,0" Click="OnOk"/>
            <Button Content="{sd:Localize Cancel}" IsCancel="True" Width="80" Click="OnCancel"/>
        </StackPanel>
    </StackPanel>
</sd:ModalWindow>
