// PSMain(ExpectedResult=#FFFFFFFF, cbuffer.Test=1)
// PSMain(ExpectedResult=#7F7F7F7F, cbuffer.Test=0)

namespace Stride.Shaders.Tests;

shader IfElse
{
    stream float4 ColorTarget : SV_Target0;

    cbuffer Test
    {
        int Test1;
    }

    void PSMain()
    {
        streams.ColorTarget = float4(0.0, 0.0, 0.0, 0.0);
        if (Test1 == 1)
            streams.ColorTarget = float4(1.0, 1.0, 1.0, 1.0);
        else
            streams.ColorTarget = float4(0.5, 0.5, 0.5, 0.5);
    }
}