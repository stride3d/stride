shader BasicMixin<float a>
{
    float myFloat = 0.2f;
    stage float3 myPosition : register(b);
    stage stream float2 screenPosition : register(vs, b);
    
    abstract void myFunc();

    float4 ComputeColor()
    {
        return float4(1.0);
    }
};