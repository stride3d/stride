trigger:
- master

pool:
  vmImage: 'VS2019-Win2016'

variables:
  solution: 'build/Stride.sln'

steps:
- task: MSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArguments: '/t:BuildWindows /p:StrideSkipUnitTests=true /p:StrideEnableCodeAnalysis=true'
